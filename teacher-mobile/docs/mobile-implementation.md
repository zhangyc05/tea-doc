# 教师手机端实现规则

本文档记录 `teacher-mobile/` 的移动端页面基准规则。当前基准页为：

```txt
页面：教师端手机｜活动｜活动首页
源码：teacher-mobile/src/pages/activity/index.vue
目标图：../效果图/教师手机端/2活动/活动首页/教师端手机｜活动｜活动首页.png
```

## 工程边界

- `teacher-mobile/` 是教师手机端唯一工程，目标交付 H5、微信小程序、App。
- `frontend/` 继续承载管理端 PC，不承载教师手机端页面。
- 手机端页面不复用管理端 PC 布局、路由和样式。
- 每次只实现一个页面；做完 2-3 个页面后再抽通用业务组件。

## 验收视口

H5 视觉验收至少覆盖：

```txt
390x844
375x812
430x932
360x800
```

多端构建验收至少执行：

```bash
npm run typecheck
npm run build:h5
npm run build:mp-weixin
```

## 页面容器

- 页面使用浅绿到浅蓝的纵向背景，顶部保留状态栏空间。
- 页面横向内边距以 `28rpx` 为基准，窄屏可收敛到 `22rpx`。
- 页面底部必须预留自定义 TabBar 和安全区空间：

```scss
padding-bottom: calc(150rpx + env(safe-area-inset-bottom));
```

## 卡片

- 主要内容卡片使用白色半透明背景。
- 基准圆角：
  - 内容卡片：`32rpx`
  - 顶部教师周期卡：`40rpx`
  - 小图标容器：`20rpx` 到 `28rpx`
- 卡片阴影保持轻量，避免重投影：

```scss
box-shadow: 0 22rpx 52rpx rgba(35, 51, 87, 0.07);
```

## 字号层级

- 页面主标题：`62rpx` 到 `70rpx`，粗体。
- 卡片标题：`36rpx` 到 `44rpx`，粗体。
- 业务主标题：`25rpx` 到 `34rpx`，粗体。
- 正文说明：`20rpx` 到 `27rpx`。
- 标签文字：`20rpx` 到 `25rpx`。
- 底部 Tab 文字：`26rpx`。

后续页面应优先沿用这些层级，不为单页单独发明新字号。

## TabBar

- 使用自定义组件：

```txt
teacher-mobile/src/components/MobileTabBar.vue
```

- 不使用 UniApp 原生 `tabBar` 配置，避免 H5、小程序和 App 视觉不一致。
- 入口顺序固定为：

```txt
待办 / 档案 / AI 助手 / 活动 / 我的
```

- 中间 `AI 助手` 使用凸起圆形入口。
- 当前 `AI 助手` 暂无独立页面时，可临时指向活动页；后续实现 AI 助手页面后再调整。

## 按钮

- 主要操作按钮使用绿色实心按钮。
- 次级操作按钮使用白底绿色描边。
- 使用 UniApp `button` 实现图标按钮、文本链接或业务按钮时，必须在当前页面或组件内显式清理默认 `margin`、`padding` 和 `::after` 边框，避免出现目标图中不存在的外框或居中偏移。
- 移动端按钮高度基准：
  - 主操作：`48rpx` 到 `56rpx`
  - 列表操作：`44rpx` 到 `56rpx`
- 页面中不要出现看起来可点击但没有业务意义的额外按钮。

## 状态标签

- 标签先使用局部样式，不急于抽组件。
- 当前基准色：
  - 紫色：教学反思
  - 蓝色：培训进修
  - 橙色：年度要求
  - 绿色：虚拟教研
- 标签高度基准：`30rpx` 到 `34rpx`。

## Wot Design Uni 使用边界

Wot Design Uni 作为多端基础组件库保留，优先用于：

- 表单输入
- 选择器
- 弹窗
- Toast / Notify
- 上传
- 日期时间
- 单选、多选、开关

基准页中的复杂首页结构、底部中间凸起 Tab、业务卡片布局，优先使用项目局部样式实现，以保证与目标图一致。

## 后续页面顺序

第二阶段不继续随机迁移页面，先把移动端框架规则定型，再按页面类型补齐基准：

```txt
1. 框架规则收口：Navbar、TabBar、页面容器、安全区、固定底部操作区
2. 表单密集页基准：优先选择证书/成果提交或编辑类页面
3. 列表筛选页基准：验证搜索、筛选、状态标签、加载更多
4. 小型组件沉淀：只抽 2-3 个页面中已经重复且稳定的结构
5. 再进入模块页迁移：档案首页、我的主页、AI 助手入口页
```

当前已完成 `活动首页`、`教学反思开始方式`、`教学反思选择学期课程`、`教学反思选择反思依据`、`教学反思反思范围`、`教学反思自主反思`、`教学反思AI引导对话`、`教学反思-AI对话`、`教学反思报告草稿编辑`、`教学反思确认成功`、`企业实践年度概览`、`企业实践年度列表`、`企业实践提交实践计划`、`企业实践实践计划确认提交`、`企业实践实践计划已通过`、`企业实践实践计划退回修改`、`企业实践修改实践计划`、`企业实践进行中详情`、`企业实践记录实践日志`、`企业实践-实践日志`、`企业实践记录已入档`、`企业实践上传实践证明`、`企业实践补充实践证明`、`企业实践补充材料已提交`、`企业实践重新补充材料`、`企业实践需要补充材料`、`企业实践补充历史实践`、`企业实践历史实践需补充材料`、`企业实践历史实践已确认`、`企业实践归档提交结果`、`企业实践修改档案信息`、`企业实践导入导出页面`、`企业实践工作流配置`、`企业实践登录历史页面`、`企业实践高级搜索页面`、`虚拟教研我的教研室`、`虚拟教研教研室邀请`、`虚拟教研教研活动列表`、`虚拟教研教研活动详情进行中`、`虚拟教研教研活动详情待确认贡献`、`虚拟教研确认教研贡献`、`虚拟教研教研贡献详情`、`虚拟教研教研贡献确认`、`虚拟教研贡献确认已提交`、`虚拟教研补充教研活动材料`、`虚拟教研阶段材料已提交`、`虚拟教研教研活动归档结果`、`虚拟教研教研活动已归档贡献已确认`、`虚拟教研补充材料已重新提交`、`培训进修首页`、`培训进修`、`培训申请`、`培训总结`、`培训材料归档提交结果`、`提交培训需求`、`培训需求提交`、`培训需求已提交`、`我已找到想参加的培训`、`待办首页`、`全部待办`、`全部动态`、`待确认记录详情`、`编辑待确认记录`、`修改已提交`。

组件沉淀只抽已经重复出现的结构，例如：

```txt
MobileTabBar
MobilePageShell
MobileCard
MobileStatusTag
MobileActionButton
```

不得为了未来页面提前泛化。

## 第一次组件沉淀

已沉淀的基础组件只覆盖三页中重复且稳定的低业务结构：

```txt
teacher-mobile/src/components/MobileNavbar.vue
teacher-mobile/src/components/MobilePageShell.vue
teacher-mobile/src/components/MobileCard.vue
teacher-mobile/src/components/MobileStatusTag.vue
teacher-mobile/src/components/MobileActionButton.vue
teacher-mobile/src/components/MobileTabBar.vue
```

- `MobileNavbar` 基于 Wot Design Uni `wd-navbar` 做项目级封装，只负责状态栏、返回、居中标题和右侧操作插槽。
- `MobilePageShell` 只负责页面背景、横向边距、底部 TabBar 和安全区预留。
- `MobileCard` 只负责白色内容卡片的边框、圆角、背景和阴影。
- `MobileStatusTag` 只负责紫、蓝、橙、绿四类轻量状态标签。
- `MobileActionButton` 只负责 UniApp `button` 默认样式清理，以及主按钮、描边按钮、文本链接三种低层按钮外壳。
- `MobileTabBar` 继续作为固定底部入口组件。

暂不抽以下结构：

```txt
状态栏
通知铃
搜索框
业务图标
首页业务卡片
记录列表行
```

这些结构仍由具体页面根据目标图局部实现，等更多页面重复后再判断是否抽象。

## 框架组件使用边界

- Navbar 优先走项目级 `MobileNavbar`，内部使用 Wot Design Uni `wd-navbar`，避免每页重复手写返回、标题和右侧操作。
- 待办二级页默认使用 `MobileNavbar`，当前已覆盖：`全部待办`、`全部动态`、`待确认记录详情`、`编辑待确认记录`、`修改已提交`。
- 后续新增待办二级页时直接使用 `MobileNavbar`；其他模块旧页面在再次修改时顺带迁移，避免一次性影响已验收页面。
- TabBar 暂不切换为 Wot Design Uni `wd-tabbar`。当前底部栏包含中间凸起 `AI 助手` 入口和目标图绑定的浮动胶囊样式，继续使用项目自定义 `MobileTabBar`。
- 表单、弹窗、上传、选择器、Toast 等通用交互优先使用 Wot Design Uni。

## 页面结构规则

- 一级 Tab 页使用 `MobilePageShell` 加 `MobileTabBar`，页面内容从顶部状态栏下方开始。
- 待办二级页使用 `MobileNavbar`，默认 `regular`，详情、编辑、提交结果等内容密集页面使用 `compact`。
- 需要底部常驻操作的页面，操作区可放在页面主体最后一个卡片内，也可固定在 TabBar 上方；固定时必须增加页面底部滚动预留，不能遮挡最后一张内容卡。
- 所有带 TabBar 页面底部必须预留：

```scss
padding-bottom: calc(150rpx + env(safe-area-inset-bottom));
```

- H5、小程序、App 均保持 `navigationStyle: custom`，不启用原生导航栏，避免不同端顶部高度和标题样式漂移。
- Wot Design Uni 原子组件可以进入页面内部，但页面壳层必须走项目级封装，避免每页直接拼 `wd-navbar`、`wd-tabbar`。

## 活动二级首页

当前活动二级首页基准：

```txt
页面：教师端手机｜活动｜培训进修｜培训进修首页
源码：teacher-mobile/src/pages/activity/training/index.vue
目标图：../效果图/教师手机端/2活动/2培训进修/教师端手机｜活动｜培训进修｜培训进修首页.png
```

- 活动二级首页使用 `MobileNavbar` 和固定底部 `MobileTabBar`。
- 顶部允许保留模块插画，但插画层级必须低于 Navbar，不得压住状态栏、返回按钮或标题。
- 主体按模块卡片组织：当前处理、推荐资源、我的记录、需求入口。
- 资源列表按钮保持右侧对齐，窄屏优先压缩按钮宽度和列表行距，不改成横向滚动。
- 底部需求入口作为当前模块的表单入口，当前承接 `提交培训需求` 页面。

## 企业实践年度概览页

当前企业实践年度概览页基准：

```txt
页面：教师端手机｜活动｜企业实践｜年度概览
源码：teacher-mobile/src/pages/activity/enterprise-overview/index.vue
目标图：../效果图/教师手机端/2活动/3企业实践/教师端手机｜活动｜企业实践｜年度概览.png
```

- 企业实践年度概览页使用 `MobileNavbar` 和固定底部 `MobileTabBar`。
- 页面承接活动首页“企业实践 / 记录实践”入口。
- 主体按年度完成进度、当前需要处理、实践记录、补充历史实践入口组织。
- 年度进度卡突出已完成天数、年度要求状态和当前提醒。
- 实践记录列表包含筛选胶囊、状态标签和右侧操作按钮；当前筛选只承担视觉基准。
- 底部补充历史实践入口放在内容流末尾，页面底部必须预留 TabBar 和安全区空间。

## 企业实践年度列表页

当前企业实践年度列表页基准：

```txt
页面：教师端手机｜活动｜企业实践｜年度列表
源码：teacher-mobile/src/pages/activity/enterprise-list/index.vue
目标图：../效果图/教师手机端/2活动/3企业实践/教师端手机｜活动｜企业实践｜年度列表.png
```

- 年度列表页延续企业实践年度概览的顶部年度进度卡和当前处理卡。
- 实践记录区域增加右上年度选择胶囊，当前只承担视觉基准，不打开空选择器。
- 筛选胶囊与记录列表放在同一白色卡片内；记录列表使用内嵌边框容器，行与行之间用浅色分割线。
- 记录状态在行内表达，例如 `进行中 | 已记录 8 天`、`需补充 | 待补充材料`，右侧只保留当前记录操作按钮。
- 底部补充历史实践入口必须可滚动到 TabBar 上方，不被固定底栏遮挡。

## 企业实践提交实践计划页

当前企业实践提交实践计划页基准：

```txt
页面：教师端手机｜活动｜企业实践｜提交实践计划
源码：teacher-mobile/src/pages/activity/enterprise-plan-submit/index.vue
目标图：../效果图/教师手机端/2活动/3企业实践/教师端手机｜活动｜企业实践｜提交实践计划.png
```

- 提交实践计划页使用 `MobileNavbar` 和固定底部 `MobileTabBar`。
- 页面作为企业实践表单密集页基准，主体按年度要求、输入区、AI 整理结果、补充资料、提交说明、底部操作组织。
- 输入区先用局部样式表达多行输入、语音、拍照、上传和附件胶囊，不提前抽上传组件。
- AI 整理结果分为基本信息和计划内容两段；基本信息使用表单行，计划内容使用内嵌表格。
- 窄屏下优先压缩字号和标签宽度，避免横向滚动；年度要求插画不得遮挡提示文字。
- 底部 `保存草稿 / 提交计划` 当前作为视觉基准，后续确认提交页实现后再接真实跳转。

## 企业实践实践计划确认提交页

当前企业实践实践计划确认提交页基准：

```txt
页面：教师端手机｜活动｜企业实践｜实践计划确认提交
源码：teacher-mobile/src/pages/activity/enterprise-plan-confirm/index.vue
目标图：../效果图/教师手机端/2活动/3企业实践/教师端手机｜活动｜企业实践｜实践计划确认提交.png
```

- 确认提交页使用 `MobileNavbar` 和固定底部 `MobileTabBar`。
- 页面用于提交核验前的二次确认，主体按提交提示、提交信息、后续流程、温馨提示、底部操作组织。
- 提交信息采用只读信息表，左侧标签固定宽度，右侧内容允许自然换行。
- 后续流程使用三步纵向流程块；窄屏下流程块必须完整可滚动，不被 TabBar 遮挡。
- 底部 `返回修改 / 提交核验` 当前作为视觉基准，后续实践计划结果页实现后再接真实跳转。

## 企业实践实践计划已通过页

当前企业实践实践计划已通过页基准：

```txt
页面：教师端手机｜活动｜企业实践｜实践计划已通过
源码：teacher-mobile/src/pages/activity/enterprise-plan-approved/index.vue
目标图：../效果图/教师手机端/2活动/3企业实践/教师端手机｜活动｜企业实践｜实践计划已通过.png
```

- 实践计划已通过页使用 `MobileNavbar` 和固定底部 `MobileTabBar`。
- 页面用于展示实践计划核验通过后的结果，主体按成功提示、本次实践计划、已确认内容、下一步、底部操作组织。
- 本次实践计划采用只读信息表，左侧标签固定宽度，右侧内容允许自然换行。
- 已确认内容用内嵌列表表达确认项和确认状态，状态文字右侧对齐。
- 下一步卡片说明记录日志和后续总结草稿的承接关系。
- 底部 `返回实践列表 / 开始记录日志` 当前作为视觉基准，后续记录实践日志页实现后再接真实跳转。

## 企业实践实践计划退回修改页

当前企业实践实践计划退回修改页基准：

```txt
页面：教师端手机｜活动｜企业实践｜实践计划退回修改
源码：teacher-mobile/src/pages/activity/enterprise-plan-rejected/index.vue
目标图：../效果图/教师手机端/2活动/3企业实践/教师端手机｜活动｜企业实践｜实践计划退回修改.png
```

- 退回修改页使用 `MobileNavbar`，不显示底部 `MobileTabBar`。
- 页面主体按退回提示、退回信息、本次实践计划、需修改内容、已提交附件、说明提示组织。
- 退回意见使用橙色浅底块突出审核意见，长文本自然换行。
- 本次实践计划为只读信息区，需修改内容使用编号、标题、说明和右侧状态标签。
- 底部 `返回实践列表 / 修改计划并重新提交` 固定在安全区上方，主按钮按目标图局部使用蓝色，不影响全局绿色主按钮。

## 企业实践修改实践计划页

当前企业实践修改实践计划页基准：

```txt
页面：教师端手机｜活动｜企业实践｜修改实践计划
源码：teacher-mobile/src/pages/activity/enterprise-plan-edit/index.vue
目标图：../效果图/教师手机端/2活动/3企业实践/教师端手机｜活动｜企业实践｜修改实践计划.png
```

- 修改实践计划页使用 `MobileNavbar` 和固定底部 `MobileTabBar`。
- 页面承接实践计划退回修改，主体按退回状态、退回信息、本次实践计划、修改输入区、AI 整理结果、补充资料、底部操作组织。
- 退回信息和本次实践计划采用并列信息卡；窄屏下改为纵向堆叠，避免内容挤压。
- 修改输入区沿用提交实践计划页的多行输入、语音、拍照、上传和附件胶囊密度。
- AI 整理结果使用基础信息和计划内容两段，底部保留 `修改内容 / 重新整理` 操作。
- 底部 `保存草稿 / 重新提交` 当前作为视觉基准，后续确认提交页或结果页接入时再补真实跳转。

## 企业实践进行中详情页

当前企业实践进行中详情页基准：

```txt
页面：教师端手机｜活动｜企业实践｜进行中详情
源码：teacher-mobile/src/pages/activity/enterprise-progress-detail/index.vue
目标图：../效果图/教师手机端/2活动/3企业实践/教师端手机｜活动｜企业实践｜进行中详情.png
```

- 进行中详情页使用 `MobileNavbar` 和固定底部 `MobileTabBar`。
- 页面用于展示实践计划已通过后的进行中状态，主体按实践概要、实践计划、今天可处理、实践日志、附件资料、AI 提示组织。
- 实践概要突出公司、岗位、时间、年度计入天数和进行中状态。
- 今天可处理卡片作为记录日志入口，当前只承担视觉入口。
- 实践日志使用时间线列表，附件资料使用横向资料条和补充资料入口。
- 底部 AI 提示说明后续会根据日志和附件生成总结草稿。

## 企业实践记录实践日志页

当前企业实践记录实践日志页基准：

```txt
页面：教师端手机｜活动｜企业实践｜记录实践日志
源码：teacher-mobile/src/pages/activity/enterprise-log-record/index.vue
目标图：../效果图/教师手机端/2活动/3企业实践/教师端手机｜活动｜企业实践｜记录实践日志.png
```

- 记录实践日志页使用 `MobileNavbar` 和固定底部 `MobileTabBar`。
- 页面承接进行中详情的“去记录”入口，主体按实践概要、日志日期、日志输入、AI 草稿、底部操作组织。
- 日志输入区保留语音、拍照、上传资料入口，只作为视觉基准，不接空交互。
- AI 草稿按实践内容、观察与收获、关联材料组织，材料行保留右侧箭头。
- 底部 `保存草稿 / 保存日志` 当前作为视觉基准，后续记录入档结果页实现后再补真实跳转。

## 企业实践实践日志页

当前企业实践实践日志页基准：

```txt
页面：教师端手机｜活动｜企业实践｜企业实践-实践日志
源码：teacher-mobile/src/pages/activity/enterprise-log-list/index.vue
目标图：../效果图/教师手机端/2活动/3企业实践/企业实践-实践日志.png
```

- 实践日志页使用 `MobileNavbar`，右侧保留筛选入口作为视觉基准。
- 页面用于展示已归档实践的日志列表，主体按实践概要、日志摘要、日志列表、归档提示和底部返回操作组织。
- 日志列表使用左侧日期时间轴和右侧日志卡片，卡片内展示标题、标签、附件数量和右侧箭头。
- 页面目标图没有底部 TabBar，本页不挂 `MobileTabBar`，底部只保留主操作按钮和安全区。
- 筛选、排序、日志详情和返回按钮当前只承担视觉入口，后续接真实流程时再补跳转。

## 企业实践记录已入档页

当前企业实践记录已入档页基准：

```txt
页面：教师端手机｜活动｜企业实践｜记录已入档
源码：teacher-mobile/src/pages/activity/enterprise-archive-success/index.vue
目标图：../效果图/教师手机端/2活动/3企业实践/教师端手机｜活动｜企业实践｜记录已入档.png
```

- 记录已入档页使用 `MobileNavbar`，页面目标图没有底部 TabBar，本页不挂 `MobileTabBar`。
- 页面承接企业实践证明材料核验通过后的结果状态，主体按成功提示、系统整理、入档材料、可用于场景组织。
- 入档材料卡展示 PDF 材料、入档时间和核验部门，材料行保留右侧箭头。
- 可用于场景使用三列图标入口，当前只承担视觉说明，不接空操作。
- 底部 `返回首页 / 查看档案详情` 固定在安全区上方，后续档案详情页实现后再补真实跳转。

## 企业实践上传实践证明页

当前企业实践上传实践证明页基准：

```txt
页面：教师端手机｜活动｜企业实践｜上传实践证明
源码：teacher-mobile/src/pages/activity/enterprise-proof-upload/index.vue
目标图：../效果图/教师手机端/2活动/3企业实践/教师端手机｜活动｜企业实践｜上传实践证明.png
```

- 上传实践证明页使用 `MobileNavbar`，页面目标图没有底部 TabBar，本页不挂 `MobileTabBar`。
- 页面承接企业实践材料补充流程，主体按上传说明、材料类型、上传方式、已上传材料和材料预览组织。
- 上传方式保留从手机上传和拍照上传两个入口，当前只承担视觉基准，不接真实上传能力。
- 已上传材料卡展示 PDF 文件、上传成功状态和预览入口。
- 底部 `重新上传 / 确定使用` 固定在安全区上方，后续补充实践证明确认页实现后再补真实跳转。

## 企业实践补充实践证明页

当前企业实践补充实践证明页基准：

```txt
页面：教师端手机｜活动｜企业实践｜补充实践证明
源码：teacher-mobile/src/pages/activity/enterprise-proof-supplement/index.vue
目标图：../效果图/教师手机端/2活动/3企业实践/教师端手机｜活动｜企业实践｜补充实践证明.png
```

- 补充实践证明页使用 `MobileNavbar` 和固定底部 `MobileTabBar`，目标图底部高亮 `AI 助手`，本页按图保留。
- 页面承接企业实践档案草稿补充材料流程，主体按档案摘要、上传方式、已上传材料、温馨提示、底部操作组织。
- 上传方式保留从手机上传和拍照上传两个入口，当前只承担视觉基准，不接真实上传能力。
- 已上传材料卡展示 PDF 文件、上传成功状态和材料预览入口。
- 底部 `返回草稿 / 确定使用` 放在 TabBar 上方，页面底部预留 TabBar 和安全区空间。

## 企业实践补充材料已提交页

当前企业实践补充材料已提交页基准：

```txt
页面：教师端手机｜活动｜企业实践｜补充材料已提交
源码：teacher-mobile/src/pages/activity/enterprise-supplement-submitted/index.vue
目标图：../效果图/教师手机端/2活动/3企业实践/教师端手机｜活动｜企业实践｜补充材料已提交.png
```

- 补充材料已提交页使用 `MobileNavbar`，页面目标图没有底部 TabBar，本页不挂 `MobileTabBar`。
- 页面承接补充实践证明提交后的结果状态，主体按成功提示、实践摘要、已提交材料、核验说明组织。
- 实践摘要保留 `待核验` 状态和系统整理后的实践单位、岗位、时间、天数。
- 已提交材料卡展示 PDF 材料、提交状态和提交时间。
- 底部 `查看成长档案 / 返回首页` 固定在安全区上方，后续档案页实现后再补真实跳转。

## 企业实践重新补充材料页

当前企业实践重新补充材料页基准：

```txt
页面：教师端手机｜活动｜企业实践｜重新补充材料
源码：teacher-mobile/src/pages/activity/enterprise-resupplement/index.vue
目标图：../效果图/教师手机端/2活动/3企业实践/教师端手机｜活动｜企业实践｜重新补充材料.png
```

- 重新补充材料页使用 `MobileNavbar`，页面目标图没有底部 TabBar，本页不挂 `MobileTabBar`。
- 页面承接补充材料被退回后的再次提交流程，主体按需补充提示、实践摘要、核验反馈、已提交材料、重新上传、补充说明和提交提醒组织。
- 需补充提示使用橙色状态，核验反馈用浅橙色说明块突出缺少单位盖章页。
- 已提交材料展示 `已退回补充` 状态；重新上传区域保留虚线上传入口，当前只承担视觉基准，不接真实上传能力。
- 底部 `稍后处理 / 重新提交` 固定在安全区上方，`重新提交` 承接补充材料已提交结果页。

## 企业实践需要补充材料页

当前企业实践需要补充材料页基准：

```txt
页面：教师端手机｜活动｜企业实践｜需要补充材料
源码：teacher-mobile/src/pages/activity/enterprise-supplement-needed/index.vue
目标图：../效果图/教师手机端/2活动/3企业实践/教师端手机｜活动｜企业实践｜需要补充材料.png
```

- 需要补充材料页使用 `MobileNavbar` 和固定底部 `MobileTabBar`，目标图底部高亮 `AI 助手`。
- 页面承接企业实践材料核验未通过状态，主体按待补充提示、实践摘要、核验反馈、已提交材料、重新补充材料、补充说明组织。
- 待补充提示和核验反馈使用橙色状态层级；实践摘要延续绿色标签和管理字段密度。
- `去补充材料` 承接重新补充材料页，`重新提交` 承接补充材料已提交结果页。
- 底部 `稍后处理 / 重新提交` 固定在 TabBar 上方，避免遮挡底部导航。

## 企业实践补充历史实践页

当前企业实践补充历史实践页基准：

```txt
页面：教师端手机｜活动｜企业实践｜补充历史实践
源码：teacher-mobile/src/pages/activity/enterprise-history-supplement/index.vue
目标图：../效果图/教师手机端/2活动/3企业实践/教师端手机｜活动｜企业实践｜补充历史实践.png
```

- 补充历史实践页使用 `MobileNavbar` 和固定底部 `MobileTabBar`，目标图底部高亮 `活动`。
- 页面承接年度概览/列表里的补充历史实践入口，主体按年度要求、历史实践输入、AI 整理结果、证明材料、提交说明组织。
- 输入区保留语音、拍照、上传资料三种视觉入口和 PDF 附件 chip；当前只承担页面还原，不接真实上传。
- AI 整理结果拆成基础信息和实践内容两段表格，保留 `修改内容 / 重新整理` 操作。
- 底部 `保存草稿 / 提交确认` 固定在 TabBar 上方，`提交确认` 跳转到 `/pages/activity/enterprise-history-confirmed/index`。

## 企业实践历史实践需补充材料页

当前企业实践历史实践需补充材料页基准：

```txt
页面：教师端手机｜活动｜企业实践｜历史实践需补充材料
源码：teacher-mobile/src/pages/activity/enterprise-history-supplement-needed/index.vue
目标图：../效果图/教师手机端/2活动/3企业实践/教师端手机｜活动｜企业实践｜历史实践需补充材料.png
```

- 历史实践需补充材料页使用 `MobileNavbar`，目标图没有底部 TabBar，本页不挂 `MobileTabBar`。
- 页面承接历史实践补充被学院退回后的材料补交状态，主体按顶部橙色退回提示、退回信息、已提交历史实践摘要、需补充材料和上传区组织。
- 需补材料清单突出 `未上传` 状态，上传区只承担视觉基准与本地提示，不接真实文件上传。
- 底部 `保存草稿 / 补充后重新提交` 固定在安全区上方，当前使用本地 toast 反馈，避免跳转到未实现页面。

## 活动培训列表页

当前活动培训列表页基准：

```txt
页面：教师端手机｜活动｜培训进修｜培训进修
源码：teacher-mobile/src/pages/activity/training-list/index.vue
目标图：../效果图/教师手机端/2活动/2培训进修/教师端手机｜活动｜培训进修｜培训进修.png
```

- 培训列表页使用 `MobileNavbar` 和固定底部 `MobileTabBar`。
- 主体按推荐培训、我的培训、提交培训需求入口三段组织。
- 推荐培训卡允许使用较大的课程图标和右下角操作按钮；按钮必须避让推荐原因文字。
- 需申请培训的操作入口承接 `培训申请` 结果页；未接真实资源打开能力前，不为“直接学习”增加空业务逻辑。
- 我的培训记录使用更紧凑的小图标、状态文字和右侧描边操作按钮，窄屏下优先压缩图标和按钮宽度；已结束培训的 `整理总结` 入口承接 `培训总结` 页面。
- “没有合适培训”入口放在底部独立卡片，作为 `提交培训需求` 页面入口。

## 活动培训总结页

当前活动培训总结页基准：

```txt
页面：教师端手机｜活动｜培训进修｜培训总结
源码：teacher-mobile/src/pages/activity/training-summary/index.vue
目标图：../效果图/教师手机端/2活动/2培训进修/教师端手机｜活动｜培训进修｜培训总结.png
```

- 培训总结页使用 `MobileNavbar`，但不显示底部 `MobileTabBar`，底部只保留固定提交栏。
- 主体按培训信息、归档准备、AI 总结草稿、证书确认、后续想法、补充材料组织。
- 证书确认在宽屏手机可三列展示，窄屏必须降为单列，避免文字和单选框挤压。
- 底部固定栏贴近安全区，包含材料状态、保存草稿、提交归档；窄屏时允许换行，第一行状态，第二行两个按钮。
- 页面底部预留必须覆盖固定提交栏高度，滚动到底部时补充材料卡不能被提交栏遮挡。
- 提交归档入口承接 `培训材料归档提交结果` 页面。

## 活动培训归档结果页

当前活动培训归档结果页基准：

```txt
页面：教师端手机｜活动｜培训进修｜培训材料归档提交结果
源码：teacher-mobile/src/pages/activity/training-archive-result/index.vue
目标图：../效果图/教师手机端/2活动/2培训进修/教师端手机｜活动｜培训进修｜培训材料归档提交结果.png
```

- 归档结果页使用 `MobileNavbar`，不显示底部 `MobileTabBar`。
- 主体按结果摘要、已提交内容、当前进度和底部操作组织。
- 结果摘要突出“培训材料已提交”和“正在等待归档确认”，并保留不发证书提示。
- 已提交内容使用紧凑五行清单，右侧状态勾选固定对齐；窄屏优先压缩标签列宽，不横向滚动。
- 当前进度使用三步横向进度线，当前节点突出为绿色圆环。
- 底部操作在内容流内展示：返回培训进修、查看提交内容、返回活动首页；页面不额外添加未实现的业务入口。

## 活动表单页

当前活动表单页基准：

```txt
页面：教师端手机｜活动｜培训进修｜提交培训需求
源码：teacher-mobile/src/pages/activity/training-demand/index.vue
目标图：../效果图/教师手机端/2活动/2培训进修/教师端手机｜活动｜培训进修｜提交培训需求.png
```

当前已覆盖同组变体：

```txt
页面：教师端手机｜活动｜培训进修｜我已找到想参加的培训
源码：teacher-mobile/src/pages/activity/training-found/index.vue
目标图：../效果图/教师手机端/2活动/2培训进修/教师端手机｜活动｜培训进修｜我已找到想参加的培训.png
```

- 活动表单页使用 `MobileNavbar` 和固定底部 `MobileTabBar`。
- 表单主体按卡片分组：需求类型、输入信息、AI 识别结果、AI 申请说明、提交后说明。
- 方向选择类表单使用双模式卡、方向标签、AI 生成说明和提交后说明组织；选中标签允许使用右下角圆形勾选标记。
- 当前页先保留局部表单样式，不抽通用表单组件；后续再做 1-2 个表单页后判断是否沉淀 `FormSection`。
- 长文本输入区必须支持语音、拍照、上传、链接四类入口的视觉占位，但未接真实能力前不要新增空业务逻辑。
- 固定底部操作区必须避让 TabBar 和安全区，按钮顺序为左侧次操作、右侧主操作；页面底部预留需覆盖 TabBar、操作区和安全区高度。
- 在 `430x932` 与 `360x800` 下不得横向溢出；滚动到底部时最后一个说明卡片不能被固定操作区遮挡。

## 活动结果页

当前活动结果页基准：

```txt
页面：教师端手机｜活动｜培训进修｜培训需求已提交
源码：teacher-mobile/src/pages/activity/training-demand-result/index.vue
目标图：../效果图/教师手机端/2活动/2培训进修/教师端手机｜活动｜培训进修｜培训需求已提交.png
```

当前已覆盖同组变体：

```txt
页面：教师端手机｜活动｜培训进修｜培训申请
源码：teacher-mobile/src/pages/activity/training-application/index.vue
目标图：../效果图/教师手机端/2活动/2培训进修/教师端手机｜活动｜培训进修｜培训申请.png
```

```txt
页面：教师端手机｜活动｜培训进修｜培训需求提交
源码：teacher-mobile/src/pages/activity/training-need-result/index.vue
目标图：../效果图/教师手机端/2活动/2培训进修/教师端手机｜活动｜培训进修｜培训需求提交.png
```

- 活动结果页使用 `MobileNavbar` 和固定底部 `MobileTabBar`。
- 结果页主体按成功状态、提交内容、当前进度、提醒说明、底部操作组织。
- 提交内容使用左右两列信息表，窄屏优先压缩标签列，不横向滚动。
- 当前进度使用三步纵向时间线，状态色只突出当前已完成节点。
- 培训需求结果页底部操作放在内容流内；培训申请结果页如果目标图要求固定三操作按钮，则固定在 TabBar 上方并增加底部滚动预留。
- `培训需求提交` 用于“我想提升某项能力”方向的提交结果；`培训需求已提交` 用于“我已找到想参加的培训”方向的提交结果。
- 结果页如果出现同业务的旧版视觉图，优先以信息更完整、结构更稳定的最终稿为基准。

## 教学反思开始方式页

当前教学反思开始方式页基准：

```txt
页面：教师端手机｜活动｜教学反思｜开始方式
源码：teacher-mobile/src/pages/activity/reflection-start/index.vue
目标图：../效果图/教师手机端/2活动/1教学反思/教师端手机｜活动｜教学反思｜开始方式.png
```

- 教学反思开始方式页使用 `MobileNavbar` 和固定底部 `MobileTabBar`。
- 页面主体按已有报告、无报告开始方式、最近草稿三段组织。
- 已有报告列表使用大图标、标题说明、可用依据标签和右侧选择按钮；当前只接视觉入口，不增加未实现的报告选择逻辑。
- 无报告开始方式包含录制课堂音频、上传教学资料、直接与 AI 对话三类入口；后续页面再分别接真实录音、上传和 AI 对话流程。
- 最近草稿用于承接后续报告草稿编辑页；在未实现编辑页前只保留视觉按钮。
- 窄屏下优先压缩图标和按钮宽度，不能让标题、按钮或底部 TabBar 相互遮挡。

## 教学反思选择学期课程页

当前教学反思选择学期课程页基准：

```txt
页面：教师端手机｜活动｜教学反思｜选择学期课程
源码：teacher-mobile/src/pages/activity/reflection-course/index.vue
目标图：../效果图/教师手机端/2活动/1教学反思/教师端手机｜活动｜教学反思｜选择学期课程.png
```

- 选择学期课程页使用 `MobileNavbar` 和固定底部 `MobileTabBar`。
- 主体按反思范围、本学期课程、可用依据、补充入口组织。
- 反思范围使用两列选项，当前选中“学期课程”，并保留绿色说明。
- 可用依据采用左侧依据类型、右侧明细列表的双栏结构；窄屏下压缩左侧类型说明，不改为横向滚动。
- 底部固定操作区显示已选数量和“开始 AI 引导反思”，必须位于 TabBar 上方并增加页面底部预留。
- 当前页面只做视觉选择基准；真实多选、展开月份和补充上传能力后续接入。

## 教学反思选择反思依据页

当前教学反思选择反思依据页基准：

```txt
页面：教师端手机｜活动｜教学反思｜选择反思依据
源码：teacher-mobile/src/pages/activity/reflection-evidence/index.vue
目标图：../效果图/教师手机端/2活动/1教学反思/教师端手机｜活动｜教学反思｜选择反思依据.png
```

- 选择反思依据页使用 `MobileNavbar` 和固定底部 `MobileTabBar`。
- 主体按反思范围、本次课、本次课可用依据、补充入口组织。
- 反思范围使用两列选项，当前选中“单次课”，并保留绿色说明。
- 可用依据使用单列大条目，左侧图标、中部说明、右侧选择状态；选中项使用浅绿色背景和绿色勾选。
- 底部固定操作区显示已选 2 份依据和“开始 AI 引导反思”，必须位于 TabBar 上方并增加页面底部预留。
- 补充入口只保留上传教学资料、录制课堂音频、直接与 AI 对话三类视觉入口；真实上传、录音、对话能力后续接入。

## 教学反思反思范围页

当前教学反思反思范围页基准：

```txt
页面：教师端手机｜活动｜教学反思｜反思范围
源码：teacher-mobile/src/pages/activity/reflection-scope/index.vue
目标图：../效果图/教师手机端/2活动/1教学反思/教师端手机｜活动｜教学反思｜反思范围.png
```

- 反思范围页使用 `MobileNavbar` 和固定底部 `MobileTabBar`。
- 主体按反思范围、阶段范围、本阶段、本阶段可用依据、补充入口组织。
- 反思范围当前选中“课程阶段”，并保留阶段反思说明。
- 阶段范围使用按周、按月、按季度、自定义四段选择，当前选中“按月”。
- 可用依据使用单列大条目，支持课堂分析、阶段测验、单元资料等不同图标色；选中项使用浅绿色背景和绿色勾选。
- 底部固定操作区显示已选 3 份依据和“开始 AI 引导反思”，必须位于 TabBar 上方并增加页面底部预留。

## 教学反思自主反思页

当前教学反思自主反思页基准：

```txt
页面：教师端手机｜活动｜教学反思｜自主反思
源码：teacher-mobile/src/pages/activity/reflection-self/index.vue
目标图：../效果图/教师手机端/2活动/1教学反思/教师端手机｜活动｜教学反思｜自主反思.png
```

- 自主反思页使用 `MobileNavbar` 和固定底部 `MobileTabBar`。
- 反思范围从两列单选改为四个图标卡，当前选中“自主反思”。
- 主体按反思范围、开始方式、关联课程、补充材料、底部操作组织。
- 开始方式为“直接和 AI 聊聊”和“上传教学资料”两张并列大卡；窄屏只能压缩字号和间距，不横向滚动。
- 关联课程为可选信息卡，包含课程封面、班级学期和更换按钮；真实课程选择后续接入。
- 补充材料入口为拍照上传、录制课堂音频、上传文件三类；未接真实能力前只保留视觉入口。
- 底部固定操作区显示已添加材料数量和“开始 AI 对话”，必须位于 TabBar 上方并增加页面底部预留。

## 教学反思 AI 引导对话页

当前教学反思 AI 引导对话页基准：

```txt
页面：教师端手机｜活动｜教学反思｜AI引导对话
源码：teacher-mobile/src/pages/activity/reflection-guide-chat/index.vue
目标图：../效果图/教师手机端/2活动/1教学反思/教师端手机｜活动｜教学反思｜AI引导对话.png
```

- AI 引导对话页使用 `MobileNavbar` 和固定底部 `MobileTabBar`。
- 主体按已选依据摘要、AI 对话、已整理反思要点组织。
- 对话区包含 AI 气泡、用户气泡和快捷选项；快捷选项只承担视觉引导，不接真实选择逻辑。
- 底部输入区位于内容流末尾，包含语音按钮、文本输入占位、添加按钮和可添加提示。
- 生成草稿操作区位于输入区下方、TabBar 上方，显示已整理要点数量和“生成反思草稿”按钮。
- 页面底部必须预留 TabBar 和安全区高度，滚动到底部时生成草稿操作区不能被遮挡。

## 教学反思 AI 对话页

当前教学反思 AI 对话页基准：

```txt
页面：教学反思-AI对话
源码：teacher-mobile/src/pages/activity/reflection-ai-chat/index.vue
目标图：../效果图/教师手机端/2活动/1教学反思/教学反思-AI对话.png
```

- AI 对话页使用 `MobileNavbar` 和固定底部 `MobileTabBar`。
- 该页承接自主反思的“未添加材料也可开始”状态，不替代已选依据后的 AI 引导对话页。
- 主体按自主反思摘要、AI 对话、AI 已整理线索、补充材料入口、输入生成区组织。
- 对话区包含多轮 AI 气泡、用户气泡和快捷选项；快捷选项只承担视觉引导，不接真实选择逻辑。
- 输入生成区位于内容流末尾，显示添加入口、输入占位、生成反思草稿按钮和已整理线索数量。
- 页面底部必须预留 TabBar 和安全区高度，滚动到底部时输入生成区不能被遮挡。

## 教学反思报告草稿编辑页

当前教学反思报告草稿编辑页基准：

```txt
页面：教师端手机｜活动｜教学反思｜报告草稿编辑
源码：teacher-mobile/src/pages/activity/reflection-draft/index.vue
目标图：../效果图/教师手机端/2活动/1教学反思/教师端手机｜活动｜教学反思｜报告草稿编辑.png
```

- 报告草稿编辑页使用 `MobileNavbar` 和固定底部 `MobileTabBar`。
- 页面承接已选依据 AI 引导对话页、自主反思 AI 对话页的“生成反思草稿”入口。
- 主体按草稿来源、AI 反思报告草稿、草稿辅助操作、确认提示、保存/确认操作组织。
- 草稿正文使用大段文本编辑框视觉，保留摘要、分节标题、项目符号和字数统计。
- 辅助操作包括“修改草稿”“补充一句想法”“重新让 AI 优化”，当前只承担视觉入口。
- 底部保存草稿和确认反思放在内容流末尾，页面底部必须预留 TabBar 和安全区空间。

## 教学反思确认成功页

当前教学反思确认成功页基准：

```txt
页面：教师端手机｜活动｜教学反思｜确认成功
源码：teacher-mobile/src/pages/activity/reflection-success/index.vue
目标图：../效果图/教师手机端/2活动/1教学反思/教师端手机｜活动｜教学反思｜确认成功.png
```

- 确认成功页使用 `MobileNavbar` 和固定底部 `MobileTabBar`。
- 页面承接报告草稿编辑页的“确认反思”入口。
- 主体按成功状态、反思摘要、后续去向、后续操作组织。
- 成功状态卡需要突出绿色确认图形和“教学反思已确认”。
- 后续操作包含查看反思详情、再做一篇反思、返回活动首页；当前只接返回活动首页真实跳转。
- 页面底部必须预留 TabBar 和安全区空间，操作卡不能被 TabBar 遮挡。

## 详情页与操作区

当前详情页基准：

```txt
页面：教师端手机｜待办｜待确认记录详情｜培训证书
源码：teacher-mobile/src/pages/todo/certificate-detail/index.vue
目标图：../效果图/教师手机端/0待办/教师端手机｜待办｜待确认记录详情｜培训证书.png
```

- 详情页使用 `MobileNavbar`，内容密集详情页优先使用 `compact`。
- 详情页仍保留底部 `MobileTabBar`，底部内容必须预留 TabBar 与安全区空间。
- 主体优先使用纵向信息卡：摘要卡、信息表、材料卡、确认卡。
- 信息表使用左侧固定标签列、右侧内容列；行高随视口压缩，但不牺牲可读性。
- 多操作按钮放在底部确认卡内，顺序为主按钮、次按钮、弱化按钮。
- 操作按钮不得贴住 TabBar；在 `430x932` 与 `360x800` 下应能完整看到确认卡的关键操作。

## 表单页与提交区

当前表单页基准：

```txt
页面：教师端手机｜待办｜编辑待确认记录
源码：teacher-mobile/src/pages/todo/certificate-edit/index.vue
目标图：../效果图/教师手机端/0待办/教师端手机｜待办｜编辑待确认记录.png
```

- 表单页使用 `MobileNavbar`，标题居中，说明文字跟随标题下方。
- 表单页可复用详情页摘要卡，但需要压缩摘要高度，避免挤占输入区域。
- 可编辑信息使用整行输入框：左侧固定字段名，中间输入值，右侧编辑图标。
- 材料区使用文件卡片和明确的“更换材料”按钮；后续接真实上传时优先使用 Wot Design Uni 上传能力。
- 补充说明使用多行文本输入，保留字数提示。
- 提交提示、主按钮、取消按钮放在 TabBar 上方；在 `430x932` 与 `360x800` 下必须完整可见，不得被 TabBar 遮挡。

## 提交结果页

当前结果页基准：

```txt
页面：教师端手机｜待办｜修改已提交｜培训证书
源码：teacher-mobile/src/pages/todo/certificate-submit/index.vue
目标图：../效果图/教师手机端/0待办/教师端手机｜待办｜修改已提交｜培训证书.png
```

- 结果页使用 `MobileNavbar`、结果说明、状态摘要、修改内容、材料、进度、下一步说明和底部操作。
- 状态摘要需要突出当前状态，例如“待核验”，并保留对应记录标题与归属分类。
- 修改内容使用对比行，左侧字段名固定，右侧展示“由旧值修改为新值”，新值用绿色强调。
- 进度条最多保持三步横向展示；窄屏下优先压缩字号和连接线，不改为纵向结构。
- 底部结果操作必须避让 TabBar，在 `430x932` 和 `360x800` 下可见且不被遮挡。

## 待办列表页

当前列表页基准：

```txt
页面：教师端手机｜待办｜全部待办
源码：teacher-mobile/src/pages/todo/all/index.vue
目标图：../效果图/教师手机端/0待办/教师端手机｜待办｜全部待办.png
```

- 列表页使用 `MobileNavbar`、分段筛选和单一卡片列表。
- 分段筛选当前只承担视觉筛选基准：全部、待确认、待补充、可完善；后续真实筛选接入时再增加状态逻辑。
- 列表行包含左侧类型图标、中部标签标题说明、右侧状态和操作按钮。
- 6 条列表在 `430x932` 下应完整露出；窄屏优先压缩行距和字号，避免横向溢出。
- 列表页可以滚动，但首屏不应出现筛选文字截断、按钮文字折行或 TabBar 遮挡关键操作。

## 动态流列表页

当前动态流基准：

```txt
页面：教师端手机｜待办｜全部动态
源码：teacher-mobile/src/pages/todo/dynamics/index.vue
目标图：../效果图/教师手机端/0待办/教师端手机｜待办｜全部动态.png
```

- 动态流使用 `MobileNavbar`、筛选入口、横向分类胶囊和按日期分组的卡片流。
- 分类胶囊包含全部、记录确认、材料更新、草稿保存、其他；当前只做视觉基准，筛选页单独实现。
- 日期分组展示左侧绿色点、分组标题和右侧数量。
- 动态卡片包含圆形图标、标题、说明、业务分类、时间和右侧箭头。
- 动态页允许纵向滚动；底部必须预留 TabBar 安全区，滚动到底部时最后一条动态应能完整露出。
- 筛选入口打开底部抽屉，不跳转独立页面。
- 筛选抽屉包含时间范围、更新类型、相关分类三组筛选项。
- 抽屉背景使用半透明遮罩，抽屉层级必须高于底部 TabBar。
- 抽屉底部操作为“重置”和“查看结果”，在 `390x844`、`375x812`、`430x932`、`360x800` 下必须完整可见。

## 企业实践历史实践已确认页

当前企业实践历史实践已确认页基准：

```txt
页面：教师端手机｜活动｜企业实践｜历史实践已确认
源码：teacher-mobile/src/pages/activity/enterprise-history-confirmed/index.vue
目标图：../效果图/教师手机端/2活动/3企业实践/教师端手机｜活动｜企业实践｜历史实践已确认.png
```

- 历史实践已确认页使用 `MobileNavbar`，目标图没有底部 TabBar，本页不挂 `MobileTabBar`。
- 页面承接历史实践补充通过后的结果状态，主体按确认成功提示、历史实践摘要、已确认内容、年度记录更新和后续可查看入口组织。
- 年度记录更新展示 `28/30 天`、进度条和还差 2 天说明。
- 底部 `返回实践列表 / 查看企业实践档案` 固定在安全区上方，当前使用本地 toast 反馈，后续相关详情页实现后再补真实跳转。

## 企业实践归档提交结果页

当前企业实践归档提交结果页基准：

```txt
页面：教师端手机｜活动｜企业实践｜归档提交结果
源码：teacher-mobile/src/pages/activity/enterprise-archive-result/index.vue
目标图：../效果图/教师手机端/2活动/3企业实践/教师端手机｜活动｜企业实践｜归档提交结果.png
```

- 归档提交结果页使用 `MobileNavbar`，目标图没有底部 TabBar，本页不挂 `MobileTabBar`。
- 页面承接实践总结与材料确认后的归档提交中间态，主体按等待确认结果、流程进度、本次实践概览和提交内容组织。
- 顶部流程进度展示 `记录实践日志 / 总结与材料确认 / 提交归档 / 审核通过` 四步，当前状态突出 `等待学院审核中`。
- 提交内容卡展示实践总结、附件资料、实践计划与日志三项，并保留审核过程消息通知提示。
- 底部 `返回实践列表 / 查看消息通知` 固定在安全区上方，当前使用本地 toast 反馈，后续消息页实现后再补真实跳转。

## 企业实践修改档案信息页

当前企业实践修改档案信息页基准：

```txt
页面：教师端手机｜活动｜企业实践｜企业实践-修改档案信息
源码：teacher-mobile/src/pages/activity/enterprise-archive-edit/index.vue
目标图：../效果图/教师手机端/2活动/3企业实践/企业实践-修改档案信息.png
```

- 修改档案信息页使用 `MobileNavbar` 和固定底部 `MobileTabBar`，目标图底部高亮 `AI 助手`。
- 页面承接 AI 整理档案草稿后的人工校对流程，主体按档案草稿信息、基本信息、实践内容、补充说明、相关材料和温馨提示组织。
- 基本信息字段以可修改行呈现，当前使用本地 toast 反馈，不跳转未实现编辑页。
- 相关材料区域展示当前未上传证明材料，并保留 `去补充材料` 视觉入口。
- 底部 `返回草稿 / 保存修改` 固定在 TabBar 上方，当前使用本地 toast 反馈，后续草稿流转页实现后再补真实跳转。

## 企业实践导入导出页

当前企业实践导入导出页基准：

```txt
页面：教师端手机｜活动｜企业实践｜导入导出页面
源码：teacher-mobile/src/pages/activity/enterprise-import-export/index.vue
目标图：../效果图/教师手机端/2活动/3企业实践/导入导出页面.png
```

- 目标图文件名为导入导出页面，实际视觉内容为“实践计划已提交 / 等待确认”的结果状态页。
- 页面使用 `MobileNavbar` 和固定底部 `MobileTabBar`，底部 TabBar 高亮 `活动`。
- 主体按提交成功提示、本次实践计划、AI 已整理内容、当前状态组织。
- 本次实践计划使用只读信息行展示实践单位、实践岗位、计划时间、预计实践天数和提交时间。
- AI 已整理内容使用四行清单展示基础信息、实践目标、实践内容和附件资料的提交状态。
- 底部 `查看提交内容 / 返回实践列表` 固定在 TabBar 上方，当前使用本地 toast 反馈，后续相关页面实现后再补真实跳转。

## 企业实践工作流配置页

当前企业实践工作流配置页基准：

```txt
页面：教师端手机｜活动｜企业实践｜工作流配置
源码：teacher-mobile/src/pages/activity/enterprise-workflow-config/index.vue
目标图：../效果图/教师手机端/2活动/3企业实践/工作流配置.png
```

- 目标图文件名为工作流配置，实际视觉内容为“日志详情 / 已归档”的实践日志详情页。
- 页面使用 `MobileNavbar`，右侧保留 `分享` 视觉入口，目标图没有底部 TabBar，本页不挂 `MobileTabBar`。
- 主体按日志摘要、所属实践、日志内容、实践要点、附件资料、AI 整理摘要、归档说明组织。
- 附件资料展示 PDF 和图片两条记录，`查看` 与 `全部下载` 当前使用本地 toast 反馈，不触发未实现下载或详情跳转。
- 底部 `返回实践日志` 位于内容流末尾，当前使用本地 toast 反馈，后续实践日志列表跳转实现后再补真实跳转。

## 企业实践登录历史页面

当前企业实践登录历史页面基准：

```txt
页面：教师端手机｜活动｜企业实践｜登录历史页面
源码：teacher-mobile/src/pages/activity/enterprise-login-history/index.vue
目标图：../效果图/教师手机端/2活动/3企业实践/登录历史页面.png
```

- 目标图文件名为登录历史页面，实际视觉内容为“实践总结与材料确认”的归档前确认页。
- 页面使用 `MobileNavbar`，目标图没有底部 TabBar，本页不挂 `MobileTabBar`。
- 主体按企业实践摘要、归档准备、AI 已整理实践总结草稿、附件资料和提交提示组织。
- `查看日志`、`修改草稿`、`重新整理`、`补充资料`、`保存草稿`、`提交归档` 当前使用本地 toast 反馈，不触发未实现跳转或提交。
- 底部 `保存草稿 / 提交归档` 固定在安全区上方，页面主体预留底部空间，四个 H5 验收视口下不得遮挡最后一张提示卡。

## 企业实践高级搜索页面

当前企业实践高级搜索页面基准：

```txt
页面：教师端手机｜活动｜企业实践｜高级搜索页面
源码：teacher-mobile/src/pages/activity/enterprise-advanced-search/index.vue
目标图：../效果图/教师手机端/2活动/3企业实践/高级搜索页面.png
```

- 目标图文件名为高级搜索页面，实际视觉内容为“补充材料”的证明材料补充表单页。
- 页面使用 `MobileNavbar`，目标图没有底部 TabBar，本页不挂 `MobileTabBar`。
- 主体按缺失材料提醒、企业实践摘要、系统已整理信息、还需要补充、上传材料、补充说明和提交说明组织。
- `上传企业实践证明`、`稍后处理`、`提交补充` 当前使用本地 toast 反馈，不触发未实现上传或提交。
- 底部 `稍后处理 / 提交补充` 固定在安全区上方，页面主体预留底部空间，四个 H5 验收视口下不得遮挡提交说明。

## 虚拟教研我的教研室页

当前虚拟教研我的教研室页基准：

```txt
页面：教师端手机｜活动｜虚拟教研｜我的教研室
源码：teacher-mobile/src/pages/activity/virtual-research-room/index.vue
目标图：../效果图/教师手机端/2活动/4虚拟教研/教师端手机｜活动｜虚拟教研｜我的教研室.png
```

- 我的教研室页使用 `MobileNavbar` 和固定底部 `MobileTabBar`，目标图底部高亮 `AI 助手`，本页 `MobileTabBar` active 设置为 `assistant`。
- 主体按顶部说明、统计总览、当前需要处理、我的教研室、最近教研活动组织。
- 统计总览展示已加入教研室、待确认贡献、待处理邀请三项；当前需要处理区展示邀请确认和贡献确认两条任务。
- 我的教研室卡展示成员、活动、进行中任务、方向标签和待确认贡献提醒。
- `查看邀请`、`确认贡献`、`进入教研室`、`全部` 当前使用本地 toast 反馈，后续详情页实现后再补真实跳转。

## 虚拟教研教研室邀请页

当前虚拟教研教研室邀请页基准：

```txt
页面：教师端手机｜活动｜虚拟教研｜教研室邀请
源码：teacher-mobile/src/pages/activity/virtual-research-invitation/index.vue
目标图：../效果图/教师手机端/2活动/4虚拟教研/教师端手机｜活动｜虚拟教研｜教研室邀请.png
```

- 教研室邀请页使用 `MobileNavbar`、固定底部确认操作区和 `MobileTabBar`，目标图底部高亮 `活动`，本页 `MobileTabBar` active 设置为 `activity`。
- 主体按邀请摘要、教研室简介、近期活动、加入后可参与事项、加入后待确认贡献提醒组织。
- 邀请摘要展示待确认邀请状态、教研室名称、共建方向、邀请人、所属组织和邀请时间。
- 近期活动展示 3 条活动日期、名称、时间和右侧箭头，加入权益展示贡献确认、会议纪要、阶段材料、科研记录四类能力。
- `暂不加入`、`确认加入` 当前使用本地 toast 反馈，后续邀请确认流程实现后再补真实跳转或提交。

## 虚拟教研教研活动列表页

当前虚拟教研教研活动列表页基准：

```txt
页面：教师端手机｜活动｜虚拟教研｜教研活动列表
源码：teacher-mobile/src/pages/activity/virtual-research-activity-list/index.vue
目标图：../效果图/教师手机端/2活动/4虚拟教研/教师端手机｜活动｜虚拟教研｜教研活动列表.png
```

- 教研活动列表页使用 `MobileNavbar`，右侧保留 `筛选` 入口；目标图没有底部 TabBar，本页不挂 `MobileTabBar`。
- 主体按学期参与概览、当前需要处理、状态筛选、活动卡列表和底部规则提醒组织。
- 学期概览展示本学期 4 次教研活动、时间范围以及待确认贡献、进行中、需补充材料、已归档四项统计。
- 活动列表展示待确认、进行中、已归档、需补充四种状态；卡片内保留会议时间、腾讯会议、教研团队、任务/归档/补充提示和右侧操作按钮。
- `筛选`、状态筛选胶囊、`去确认`、各活动操作按钮、`了解规则` 当前使用本地 toast 反馈，后续筛选抽屉和详情页实现后再补真实跳转或筛选逻辑。
## 虚拟教研教研活动详情进行中页

当前虚拟教研教研活动详情进行中页基准：

```txt
页面：教师端手机｜活动｜虚拟教研｜教研活动详情｜进行中
源码：teacher-mobile/src/pages/activity/virtual-research-activity-detail-ongoing/index.vue
目标图：../效果图/教师手机端/2活动/4虚拟教研/教师端手机｜活动｜虚拟教研｜教研活动详情｜进行中.png
```

- 教研活动详情进行中页使用 `MobileNavbar`，右侧保留更多入口；目标图没有底部 TabBar，本页不挂 `MobileTabBar`。
- 页面主体按进行中状态胶囊、活动信息卡、活动说明、我的任务、会议安排、过程材料和系统提示组织。
- 活动信息卡展示课程案例共创碰头会、会议时间、腾讯会议会议号和所属虚拟教研室，并使用右侧会议插画承载视觉重心。
- 我的任务展示案例素材准备、进行中状态和截止时间；会议安排展示线上会议、主持人和 `进入会议` 操作。
- 过程材料展示会议纪要、任务分工、阶段材料、个人贡献四类状态，底部固定 `返回教研室` 操作当前使用本地 toast 反馈。
## 虚拟教研教研活动详情待确认贡献页

当前虚拟教研教研活动详情待确认贡献页基准：

```txt
页面：教师端手机｜活动｜虚拟教研｜教研活动详情｜待确认贡献
源码：teacher-mobile/src/pages/activity/virtual-research-activity-detail-confirm/index.vue
目标图：../效果图/教师手机端/2活动/4虚拟教研/教师端手机｜活动｜虚拟教研｜教研活动详情｜待确认贡献.png
```

- 教研活动详情待确认贡献页使用 `MobileNavbar`，右侧保留更多入口；目标图没有底部 TabBar，本页不挂 `MobileTabBar`。
- 页面主体按顶部确认提醒、活动信息卡、系统已整理、待确认贡献、已有材料、底部提示和双固定操作组织。
- 活动信息卡展示待确认贡献状态、课程案例共创碰头会、会议时间、腾讯会议会议号和所属虚拟教研室。
- 系统已整理区展示会议纪要、任务分工、阶段材料和已识别 3 项个人贡献；待确认贡献区展示 3 条贡献来源、说明和已关联依据状态。
- 已有材料区展示 pdf、docx、xlsx 等归档材料；`更多`、`查看全部`、`继续补充`、`确认贡献` 当前使用本地 toast 反馈。
## 虚拟教研确认教研贡献页

当前虚拟教研确认教研贡献页基准：

```txt
页面：教师端手机｜活动｜虚拟教研｜确认教研贡献
源码：teacher-mobile/src/pages/activity/virtual-research-confirm-contribution/index.vue
目标图：../效果图/教师手机端/2活动/4虚拟教研/教师端手机｜活动｜虚拟教研｜确认教研贡献.png
```

- 确认教研贡献页使用 `MobileNavbar`，目标图没有底部 TabBar，本页不挂 `MobileTabBar`。
- 页面主体按活动概要、AI 识别到的贡献、可补充说明、确认后去向、补充遗漏入口和底部双操作组织。
- 活动概要展示智能制造课程资源共建研讨、会议时间、腾讯会议、智能制造教研室和待确认贡献状态。
- AI 识别到的贡献展示设备调试案例整理、发言与案例补充两项贡献，并展示会议纪要、任务分工、聊天记录、发言摘录等依据标签。
- 可补充说明区保留文本输入和语音、拍照、上传入口；`补充遗漏内容`、`不是我的`、`确认贡献` 当前使用本地 toast 反馈。
## 虚拟教研教研贡献详情页

当前虚拟教研教研贡献详情页基准：

```txt
页面：教师端手机｜活动｜虚拟教研｜教研贡献详情
源码：teacher-mobile/src/pages/activity/virtual-research-contribution-detail/index.vue
目标图：../效果图/教师手机端/2活动/4虚拟教研/教师端手机｜活动｜虚拟教研｜教研贡献详情.png
```

- 教研贡献详情页使用 `MobileNavbar`，目标图没有底部 TabBar，本页不挂 `MobileTabBar`。
- 页面主体按贡献标题、所属教研活动、贡献说明、系统依据、关联材料、确认记录、记录去向和底部返回按钮组织。
- 顶部展示设备调试案例整理、已确认状态和查看已确认个人教研贡献说明。
- 系统依据展示会议纪要、任务分工、阶段材料、发言摘录四项已归档依据；关联材料展示两个 PDF 和一个 PNG 文件入口。
- `关联材料` 行和 `返回教研记录` 当前使用本地 toast 反馈。

## 虚拟教研教研贡献确认页

当前虚拟教研教研贡献确认页基准：

```txt
页面：教师端手机｜活动｜虚拟教研｜教研贡献确认
源码：teacher-mobile/src/pages/activity/virtual-research-contribution-confirm/index.vue
目标图：../效果图/教师手机端/2活动/4虚拟教研/教师端手机｜活动｜虚拟教研｜教研贡献确认.png
```

- 教研贡献确认页使用 `MobileNavbar` 和 `MobileTabBar`，目标图底部活动 Tab 处于选中状态。
- 页面主体按贡献待确认提示、活动信息、系统已整理、待确认个人贡献、本次已关联材料和底部确认操作组织。
- 系统已整理区展示会议纪要、阶段材料、个人贡献、确认后进入四项状态；待确认贡献区展示 2 项已识别个人贡献。
- 底部固定操作栏包含 `补充遗漏` 和 `确认贡献`，位于 `MobileTabBar` 上方，页面主体预留双层底部空间。
- `补充遗漏或材料`、关联材料行、底部操作按钮当前使用本地 toast 反馈，不触发未实现跳转或提交。

## 虚拟教研贡献确认已提交页

当前虚拟教研贡献确认已提交页基准：

```txt
页面：教师端手机｜活动｜虚拟教研｜贡献确认已提交
源码：teacher-mobile/src/pages/activity/virtual-research-contribution-submitted/index.vue
目标图：../效果图/教师手机端/2活动/4虚拟教研/教师端手机｜活动｜虚拟教研｜贡献确认已提交.png
```

- 贡献确认已提交页使用 `MobileNavbar`，目标图没有底部 TabBar，本页不挂 `MobileTabBar`。
- 页面主体按确认成功提示、本次教研活动、已确认的个人贡献、关联材料、当前状态、归档后可查看和底部操作组织。
- 顶部右侧展示 `等待归档` 状态；成功提示说明当前等待教研活动归档，归档后进入成长档案。
- 已确认贡献展示 2 项已提交贡献；关联材料展示两个 PDF 和一个 DOCX 文件及大小信息。
- 底部固定操作栏包含 `返回教研室` 和 `查看提交内容`，页面主体预留安全区空间；材料行和底部按钮当前使用本地 toast 反馈。

## 虚拟教研补充教研活动材料页

当前虚拟教研补充教研活动材料页基准：

```txt
页面：教师端手机｜活动｜虚拟教研｜补充教研活动材料
源码：teacher-mobile/src/pages/activity/virtual-research-supplement-material/index.vue
目标图：../效果图/教师手机端/2活动/4虚拟教研/教师端手机｜活动｜虚拟教研｜补充教研活动材料.png
```

- 补充教研活动材料页使用 `MobileNavbar` 和 `MobileTabBar`，目标图底部活动 Tab 处于选中状态。
- 页面主体按本次教研活动、退回说明、需要补充、已有依据、补充材料和底部提交操作组织。
- 需要补充区展示 `设备调试案例整理` 与 `发言与案例补充` 两项；已有依据展示会议纪要、任务分工、发言摘录三项已同步依据。
- 补充材料区包含上传资料、拍照、语音说明三个入口、补充说明输入框、字数计数和 AI 已整理补充说明。
- 底部固定操作栏包含 `保存草稿` 和 `重新提交`，位于 `MobileTabBar` 上方，页面主体预留双层底部空间；入口和按钮当前使用本地 toast 反馈。
## 虚拟教研个人资料完善页

当前虚拟教研个人资料完善页基准：

```txt
页面：个人资料完善（效果图实际标题为：教研室详情）
源码：teacher-mobile/src/pages/activity/virtual-research-profile-complete/index.vue
目标图：../效果图/教师手机端/2活动/4虚拟教研/个人资料完善.png
```

- 目标图文件名为 `个人资料完善.png`，但画面主体是 `教研室详情`，本页按目标图实现教研室详情状态。
- 页面使用 `MobileNavbar` 和 `MobileTabBar`，底部 TabBar 当前高亮 `AI 助手`，与目标图中心按钮高亮一致。
- 顶部教研室卡展示智能制造课程虚拟教研室、已加入状态、成员/活动/任务统计、简介和 3 个方向标签。
- 当前需要处理区域展示 `智能制造课程资源共建研讨` 待确认贡献卡，提供 `确认贡献` 按钮。
- 教研活动区域展示 3 条活动记录，分别覆盖待确认、已归档、进行中状态，并保留查看详情/查看记录按钮。
- 过程材料区域展示会议纪要、任务分工、阶段材料、个人贡献四类入口；底部展示 AI 整理提示。

## 虚拟教研基本信息编辑页

当前虚拟教研基本信息编辑页基准：

```txt
页面：基本信息编辑（效果图实际标题为：贡献确认已提交）
源码：teacher-mobile/src/pages/activity/virtual-research-basic-info-edit/index.vue
目标图：../效果图/教师手机端/2活动/4虚拟教研/基本信息编辑.png
```

- 目标图文件名为 `基本信息编辑.png`，但画面主体是 `贡献确认已提交`，本页按目标图实现贡献提交后的等待归档状态。
- 页面使用 `MobileNavbar` 和 `MobileTabBar`，底部 TabBar 当前高亮 `AI 助手`。
- 顶部结果卡展示剪贴板确认插画、`等待归档` 状态和归档流程说明。
- 本次教研活动卡展示 `智能制造课程资源共建研讨`、会议时间、腾讯会议和所属虚拟教研室。
- 已确认贡献区域展示 `设备调试案例整理`、`发言与案例补充` 两条记录，行点击当前使用本地 toast 反馈，不跳转未实现详情页。
- 当前状态卡展示 `教研活动归档中` 和归档完成后可在教研记录中查看的提示；底部 `返回教研室 / 查看提交内容` 当前使用本地 toast 反馈。

## 虚拟教研个人简介编辑页

当前虚拟教研个人简介编辑页基准：

```txt
页面：个人简介编辑（效果图实际标题为：贡献确认已提交）
源码：teacher-mobile/src/pages/activity/virtual-research-profile-intro-edit/index.vue
目标图：../效果图/教师手机端/2活动/4虚拟教研/个人简介编辑.png
```

- 目标图文件名为 `个人简介编辑.png`，但画面主体是 `贡献确认已提交` 等待归档状态，本页按目标图实现完整贡献提交结果页。
- 页面使用 `MobileNavbar`，右侧保留 `进度跟踪` 入口；目标图没有底部 TabBar，本页不挂载 `MobileTabBar`。
- 顶部结果区展示绿色确认状态、等待归档说明和右侧文件夹时钟插画。
- 本次教研活动卡展示 `课程案例共创碰头会`、会议时间、腾讯会议 ID 和所属虚拟教研室。
- 已确认个人贡献区域展示 3 项贡献，其中第 3 项带 `新补充` 标记，右侧展示 `已确认` 状态和文档入口。
- 关联材料区域以 2 列网格展示 PDF、DOCX、XLSX 四项材料；当前状态区域展示四步流程进度和归档后用途提示。
- `进度跟踪`、贡献行、材料行、底部 `返回教研室 / 查看提交内容` 当前使用本地 toast 反馈，不触发未实现跳转。

## 虚拟教研教师档案详情页

当前虚拟教研教师档案详情页基准：

```txt
页面：教师档案详情（效果图实际标题为：最近更新）
源码：teacher-mobile/src/pages/activity/virtual-research-teacher-archive-detail/index.vue
目标图：../效果图/教师手机端/2活动/4虚拟教研/教师档案详情.png
```

- 目标图文件名为 `教师档案详情.png`，但画面主体是 `最近更新`，本页按目标图实现档案最近更新列表。
- 页面使用 `MobileNavbar`，右侧保留 `筛选` 入口；目标图没有底部 TabBar，本页不挂载 `MobileTabBar`。
- 顶部使用五项胶囊筛选：全部、记录确认、材料更新、草稿保存、其他；当前默认高亮 `全部`。
- 列表按 `今天`、`昨天`、`更早` 三组展示，分别显示右侧条数。
- 更新记录覆盖培训证书确认、培训学时记录、教学反思草稿、精品课程成果确认、企业实践更新、材料补充完成等状态。
- `筛选`、筛选项和记录行当前使用本地 toast 反馈，不触发未实现跳转或空操作。

## 虚拟教研补充材料已重新提交页

当前虚拟教研补充材料已重新提交页基准：

```txt
页面：教师端手机｜活动｜虚拟教研｜补充材料已重新提交
源码：teacher-mobile/src/pages/activity/virtual-research-resubmitted/index.vue
目标图：../效果图/教师手机端/2活动/4虚拟教研/教师端手机｜活动｜虚拟教研｜补充材料已重新提交.png
```

- 页面使用 `MobileNavbar`，目标图没有底部 TabBar，本页不挂载 `MobileTabBar`；右上角保留更多入口。
- 顶部反馈区展示 `补充材料已重新提交`、说明文案和橙色 `状态： 等待归档`。
- 主体按本次教研活动、本次补充内容、已有依据、当前状态组织；本次补充内容包含 `设备调试案例整理` 和 `发言与案例补充` 两项，状态均为 `已补充`。
- 已有依据固定展示会议纪要、任务分工、发言摘录三项，状态均为 `已同步`。
- 当前状态使用两段时间线说明材料已收到、归档完成后将形成教研活动记录，并展示橙色提示条。
- 底部固定操作栏包含 `返回教研室` 和 `查看提交内容`，页面主体预留底部空间；更多、补充内容行与底部按钮当前使用本地 toast 反馈。

## 虚拟教研教研活动已归档贡献已确认页

当前虚拟教研教研活动已归档贡献已确认页基准：

```txt
页面：教师端手机｜活动｜虚拟教研｜教研活动已归档｜贡献已确认
源码：teacher-mobile/src/pages/activity/virtual-research-archived-confirmed/index.vue
目标图：../效果图/教师手机端/2活动/4虚拟教研/教师端手机｜活动｜虚拟教研｜教研活动已归档｜贡献已确认.png
```

- 页面使用 `MobileNavbar`，目标图没有底部 TabBar，本页不挂载 `MobileTabBar`。
- 顶部归档摘要卡展示剪贴板确认插画、`本次教研活动已完成归档`、`贡献已确认` 与 `已归档` 状态。
- 主体按本次教研活动、已确认贡献、归档材料、归档去向组织；已确认贡献固定展示 3 项，其中第三项带 `新补充` 标识。
- 归档材料展示会议纪要、发言摘录、阶段材料、任务分工记录四项，状态均为 `已归档`。
- 底部固定操作栏包含 `返回教研室` 和 `查看教研记录`，页面主体预留底部空间；材料行与底部按钮当前使用本地 toast 反馈。

## 虚拟教研教研活动归档结果页

当前虚拟教研教研活动归档结果页基准：

```txt
页面：教师端手机｜活动｜虚拟教研｜教研活动归档结果
源码：teacher-mobile/src/pages/activity/virtual-research-archive-result/index.vue
目标图：../效果图/教师手机端/2活动/4虚拟教研/教师端手机｜活动｜虚拟教研｜教研活动归档结果.png
```

- 教研活动归档结果页使用 `MobileNavbar`，目标图没有底部 TabBar，本页不挂载 `MobileTabBar`。
- 顶部成功区展示 `教研活动已归档` 和 `已归档` 状态，说明个人贡献已确认并形成教研活动记录。
- 页面主体按本次教研活动、已确认的个人贡献、归档材料、归档去向和归档提示组织。
- 已确认个人贡献展示两项贡献来源与说明；归档材料展示会议纪要、案例素材、阶段任务分工记录和个人贡献记录四项。
- 底部固定操作栏包含 `返回教研室` 和 `查看教研记录`，页面主体预留底部空间；更多、材料行和底部按钮当前使用本地 toast 反馈。

## 虚拟教研教研活动归档结果版本一页

当前虚拟教研教研活动归档结果版本一页基准：

```txt
页面：教研活动归档结果-版本一
源码：teacher-mobile/src/pages/activity/virtual-research-archive-result-v1/index.vue
目标图：../效果图/教师手机端/2活动/4虚拟教研/教研活动归档结果-版本一.png
```

- 本页是 `教研活动归档结果` 的独立视觉变体，顶部标题为 `教研活动`，副标题提示查看归档结果与个人贡献记录。
- 页面使用 `MobileNavbar`、底部 `MobileTabBar` 和固定双按钮区，目标图底部居中高亮 `AI 助手`，本页 `MobileTabBar` active 设置为 `assistant`。
- 主体按成功归档摘要、本次教研活动、已确认贡献、归档材料、归档去向五段组织。
- 已确认贡献展示 `设备调试案例整理`、`发言与案例补充` 两条记录；归档材料展示会议纪要、任务分工、阶段材料、个人贡献四项状态。
- `返回教研室 / 查看教研记录` 以及列表行当前仅本地 toast 反馈，不触发未实现跳转。

## 虚拟教研阶段材料已提交页

当前虚拟教研阶段材料已提交页基准：

```txt
页面：教师端手机｜活动｜虚拟教研｜阶段材料已提交
源码：teacher-mobile/src/pages/activity/virtual-research-stage-submitted/index.vue
目标图：../效果图/教师手机端/2活动/4虚拟教研/教师端手机｜活动｜虚拟教研｜阶段材料已提交.png
```

- 阶段材料已提交页使用 `MobileNavbar`，目标图没有底部 TabBar，本页不挂载 `MobileTabBar`。
- 顶部右侧展示 `活动进行中` 状态；成功区展示提交已保存，并说明当前教研活动仍在进行中。
- 页面主体按本次教研活动、已提交材料、补充说明、AI 已整理材料说明、当前状态和后续可能需要你处理组织。
- 已提交材料展示 `设备调试案例素材.pdf`、文件大小与提交时间；补充说明和 AI 说明展示提交材料的教学用途。
- 底部固定操作栏包含 `返回教研室` 和 `查看活动详情`，页面主体预留底部空间；材料行、详情入口和底部按钮当前使用本地 toast 反馈。
## 虚拟教研岗位管理页面

当前虚拟教研岗位管理页面基准：

```txt
页面：岗位管理页面（效果图实际标题为：已加入教研室）
源码：teacher-mobile/src/pages/activity/virtual-research-position-management/index.vue
目标图：../效果图/教师手机端/2活动/4虚拟教研/岗位管理页面.png
```

- 目标图文件名为 `岗位管理页面.png`，但画面主体是 `已加入教研室`，本页按目标图实现加入教研室后的状态页。
- 页面使用 `MobileNavbar` 和 `MobileTabBar`，底部 TabBar 当前高亮 `AI 助手`。
- 顶部教研室卡展示已加入状态、智能制造课程虚拟教研室、方向、所属组织、加入时间和加入后说明。
- `接下来可处理` 区展示 `设备调试案例整理` 待确认贡献任务，保留会议纪要、任务分工来源标签。
- `加入后可查看` 区展示教研活动、会议纪要、阶段材料、个人贡献四个入口。
- `当前提醒` 展示 1 项个人贡献待确认；底部 `返回我的教研室 / 去确认贡献` 当前使用本地 toast 反馈，不触发未实现跳转。

## 虚拟教研工作经历管理页面

当前虚拟教研工作经历管理页面基准：

```txt
页面：工作经历管理（效果图实际标题为：贡献详情）
源码：teacher-mobile/src/pages/activity/virtual-research-work-experience-management/index.vue
目标图：../效果图/教师手机端/2活动/4虚拟教研/工作经历管理.png
```

- 目标图文件名为 `工作经历管理.png`，但画面主体是 `贡献详情`，本页按目标图实现已确认贡献详情页。
- 页面使用 `MobileNavbar` 和 `MobileTabBar`，底部 TabBar 当前高亮 `活动`。
- 顶部贡献卡展示 `设备调试案例整理`、`已确认` 状态和已作为教研过程记录查看的说明。
- `所属教研活动` 展示智能制造课程资源共建研讨、时间、组织和腾讯会议方式。
- `贡献说明` 展示案例整理和资源共建标签；`系统确认依据` 展示会议纪要、任务分工、阶段材料、发言摘录四项归档依据。
- `关联材料` 展示 DOCX、PDF、PNG 三项查看入口；`确认记录` 与 `记录去向` 采用双列卡片展示。
- 底部 `返回教研记录` 固定在 `MobileTabBar` 上方，当前使用本地 toast 反馈，不触发未实现跳转。

## 虚拟教研技能特长管理页面

当前虚拟教研技能特长管理页面基准：

```txt
页面：技能特长管理（效果图实际标题为：补充遗漏贡献）
源码：teacher-mobile/src/pages/activity/virtual-research-skill-management/index.vue
目标图：../效果图/教师手机端/2活动/4虚拟教研/技能特长管理.png
```

- 目标图文件名为 `技能特长管理.png`，但画面主体是 `补充遗漏贡献`，本页按目标图实现补充系统未识别个人教研贡献的表单页。
- 页面使用 `MobileNavbar`，目标图没有底部 TabBar，本页不挂载 `MobileTabBar`。
- 顶部展示 `待确认` 状态和补充说明；本次教研活动卡展示课程案例共创碰头会、时间、腾讯会议和所属虚拟教研室。
- `系统已识别贡献` 展示两条已识别内容；`我要补充的贡献` 展示贡献名称、补充说明和输入提示。
- `AI 已整理贡献说明` 展示整理后的贡献说明；`关联依据` 展示会议纪要、发言摘录、阶段材料、任务分工四类依据。
- `补充材料` 展示上传资料、拍照、语音说明三个入口；底部 `保存草稿 / 提交补充` 当前使用本地 toast 反馈，不触发未实现提交。

## 虚拟教研获奖情况管理页面

当前虚拟教研获奖情况管理页面基准：

```txt
页面：获奖情况管理（效果图实际标题为：提交阶段材料）
源码：teacher-mobile/src/pages/activity/virtual-research-award-management/index.vue
目标图：../效果图/教师手机端/2活动/4虚拟教研/获奖情况管理.png
```

- 目标图文件名为 `获奖情况管理.png`，但画面主体是 `提交阶段材料`，本页按目标图实现教研活动阶段材料提交页。
- 页面使用 `MobileNavbar`，右侧展示 `待提交` 状态；目标图没有底部 TabBar，本页不挂载 `MobileTabBar`。
- 顶部活动卡展示课程案例共创碰头会、会议时间、腾讯会议号和所属虚拟教研室，并保留右侧会议插画。
- `我的任务` 展示案例素材准备任务、进行中状态和查看详情入口；`上传材料` 展示上传资料、拍照、语音说明三类入口。
- 已上传材料展示 `设备调试案例素材.pdf` 和文件大小；补充说明、AI 已整理材料说明、提交后说明按目标图组织。
- 底部 `保存草稿 / 提交材料` 当前使用本地 toast 反馈，不触发未实现上传或提交。

## 虚拟教研教研活动详情补充已提交页

当前虚拟教研教研活动详情补充已提交页基准：

```txt
页面：教师端手机｜活动｜虚拟教研｜教研活动详情｜补充已提交
源码：teacher-mobile/src/pages/activity/virtual-research-activity-detail-supplement-submitted/index.vue
目标图：../效果图/教师手机端/2活动/4虚拟教研/教师端手机｜活动｜虚拟教研｜教研活动详情｜补充已提交.png
```

- 页面使用 `MobileNavbar`，右侧保留更多操作点；目标图没有底部 TabBar，本页不挂载 `MobileTabBar`。
- 顶部为补充提交成功态，展示 `补充已提交` 与 `待再次确认` 状态，并提示返回贡献确认页完成完整个人贡献确认。
- 活动卡展示课程案例共创碰头会、会议时间、腾讯会议会议号和所属虚拟教研室，并保留右侧会议插画。
- `本次补充贡献` 展示新补充的课程案例结构讨论贡献，包含 AI 已整理说明和贡献描述。
- `已识别贡献（共 3 项）`、`关联依据` 和 `当前状态` 按目标图组织；底部固定 `继续补充 / 去确认贡献`，当前仅本地 toast 反馈，不触发未实现跳转。

## 虚拟教研角色分配页面

当前虚拟教研角色分配页面基准：

```txt
页面：角色分配页面（效果图实际标题为：我的教研室）
源码：teacher-mobile/src/pages/activity/virtual-research-role-assignment/index.vue
目标图：../效果图/教师手机端/2活动/4虚拟教研/角色分配页面.png
```

- 目标图文件名为 `角色分配页面.png`，但画面主体是 `我的教研室` 聚合首页，本页按目标图实现教研室状态聚合页。
- 页面使用 `MobileNavbar` 和 `MobileTabBar`，底部 TabBar 当前高亮 `AI 助手`。
- 顶部统计卡展示已加入教研室、待确认贡献和待处理邀请三类数量。
- `当前需要处理` 展示教研室邀请和待确认贡献两个任务卡；按钮当前仅本地 toast 反馈，不触发未实现跳转。
- `我的教研室` 展示已加入的智能制造教研室和邀请中的课程思政教研室；`最近教研活动` 展示待确认、已归档、进行中三类活动状态。

## 虚拟教研高级设置页面

当前虚拟教研高级设置页面基准：

```txt
页面：高级设置页面（效果图实际标题为：2026 年度发展报告）
源码：teacher-mobile/src/pages/activity/virtual-research-advanced-settings/index.vue
目标图：../效果图/教师手机端/2活动/4虚拟教研/高级设置页面.png
```

- 目标图文件名为 `高级设置页面.png`，但画面主体是 `2026 年度发展报告`，本页按目标图实现年度发展报告草稿页。
- 页面使用 `MobileNavbar`，右侧保留导出/分享图标；目标图没有底部 TabBar，本页不挂载 `MobileTabBar`。
- 顶部展示 `AI 已生成草稿`、基于 38 条已入档记录整理、更新时间和报告用途提示。
- 主体按 `成长概况`、`主要成果`、`能力发展雷达`、`下一步发展建议` 四段组织；雷达图用 CSS 近似呈现能力维度分布。
- 底部固定 `引用记录 / 重新生成 / 确认保存报告`，当前仅本地 toast 反馈，不触发未实现保存或导出。

## 活动首页教研室入口状态页

当前活动首页教研室入口状态页基准：

```txt
页面：教师端手机｜活动｜活动首页｜教研室入口状态
源码：teacher-mobile/src/pages/activity/room-entry-state/index.vue
目标图：../效果图/教师手机端/2活动/活动首页/教师端手机｜活动｜活动首页｜教研室入口状态.png
```

- 本页是活动首页的独立状态预览页，保留原 `pages/activity/index` 作为已完成首页基准。
- 页面使用 `MobilePageShell` 和 `MobileTabBar`，底部高亮 `活动`，并保留居中 `AI 助手` 入口视觉。
- 顶部展示 `活动`、通知红点、林老师年度发展周期卡；今日可开始区展示教学反思入口和三类开始依据。
- 成长活动区展示培训进修、企业实践、虚拟教研三类入口；虚拟教研状态为 `我的教研室`，说明已加入 1 个教研室且 2 项个人贡献待确认。
- 最近活动展示教学反思、培训学习记录、企业实践日志三条动态；无明确落地页的按钮只做本地 toast。

## 待办入档成功培训证书页

当前待办入档成功培训证书页基准：

```txt
页面：教师端手机｜待办｜入档成功｜培训证书
源码：teacher-mobile/src/pages/todo/certificate-archive-success/index.vue
目标图：../效果图/教师手机端/0待办/教师端手机｜待办｜入档成功｜培训证书.png
```

- 页面使用 `MobileNavbar`、`MobilePageShell` 和底部 `MobileTabBar`，底部高亮 `待办`。
- 顶部成功横幅展示 `已入档成功`，说明培训证书已确认属于本人并入档到 `个人发展`。
- `确认记录` 展示职业院校教师数字素养提升培训证书、个人发展、已入档和日期 `06.14`。
- `接下来` 展示个人发展查看、成长档案沉淀、信息更正三条说明；说明行当前仅本地 toast。
- 底部 `查看个人发展` 当前使用本地 toast，`返回待办` 跳转到 `/pages/todo/index`。

## 虚拟教研教研活动详情补充已提交页修复记录

当前修复基准：

```txt
页面：教师端手机｜活动｜虚拟教研｜教研活动详情｜补充已提交
源码：teacher-mobile/src/pages/activity/virtual-research-activity-detail-supplement-submitted/index.vue
目标图：../效果图/教师手机端/2活动/4虚拟教研/教师端手机｜活动｜虚拟教研｜教研活动详情｜补充已提交.png
```

- 已将页面内乱码文案恢复为目标图中文，包括 `补充已提交`、`待再次确认`、活动信息、补充贡献、已识别贡献、关联依据、当前状态和底部操作。
- 页面继续使用自绘成功态、活动卡、补充贡献卡、贡献列表、横向依据文件列表和固定底部操作区。
- 底部 `继续补充`、`去确认贡献` 以及状态行当前使用本地 toast 反馈，不触发未实现跳转。

## 待办已移出待确认页

当前待办已移出待确认页基准：

```txt
页面：教师端手机｜待办｜已移出待确认页
源码：teacher-mobile/src/pages/todo/certificate-removed/index.vue
目标图：../效果图/教师手机端/0待办/教师端手机｜待办｜已移出待确认页.png
路由：/pages/todo/certificate-removed/index
```

- 页面使用 `MobileNavbar`、`MobilePageShell` 和底部 `MobileTabBar`，底部高亮 `待办`。
- 顶部结果卡展示 `已移出待确认`，说明该记录已从待确认记录移出且不会进入成长档案。
- 记录卡展示 `职业院校教师数字素养提升培训证书`、`个人发展`、`培训进修`、`原待确认` 和来源 `部门导入培训名单`。
- 后续说明展示系统不再放入待确认记录，以及处理有误可联系部门重新核验两条说明。
- 底部 `返回待办` 跳转到 `/pages/todo/index`，`查看其他待确认记录` 跳转到 `/pages/todo/all/index`。
